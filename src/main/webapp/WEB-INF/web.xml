<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0"
         metadata-complete="false">

    <display-name>QR Attendance System</display-name>
    <description>Dynamic QR-based attendance management system</description>

    <!-- ========================================== -->
    <!-- CONTEXT PARAMETERS -->
    <!-- ========================================== -->
    <context-param>
        <param-name>app.version</param-name>
        <param-value>1.0.0</param-value>
    </context-param>

    <!-- ========================================== -->
    <!-- SESSION CONFIGURATION -->
    <!-- ========================================== -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure> <!-- Set to true in production with HTTPS -->
        </cookie-config>
    </session-config>

    <!-- ========================================== -->
    <!-- WELCOME FILE LIST -->
    <!-- ========================================== -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
        <welcome-file>login</welcome-file>
    </welcome-file-list>

    <!-- ========================================== -->
    <!-- ERROR PAGES -->
    <!-- ========================================== -->
    <error-page>
        <error-code>404</error-code>
        <location>/views/auth/error.jsp</location>
    </error-page>

    <error-page>
        <error-code>403</error-code>
        <location>/views/auth/error.jsp</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/views/auth/error.jsp</location>
    </error-page>

    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/views/auth/error.jsp</location>
    </error-page>

    <!-- ========================================== -->
    <!-- FILTERS -->
    <!-- ========================================== -->

    <!-- Encoding Filter (must be first) -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>com.attendance.filters.EncodingFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Authentication Filter -->
    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>com.attendance.filters.AuthenticationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
        <url-pattern>/staff/*</url-pattern>
        <url-pattern>/student/*</url-pattern>
    </filter-mapping>

    <!-- Authorization Filter (Role-based access) -->
    <filter>
        <filter-name>AuthorizationFilter</filter-name>
        <filter-class>com.attendance.filters.AuthorizationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthorizationFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
        <url-pattern>/staff/*</url-pattern>
        <url-pattern>/student/*</url-pattern>
    </filter-mapping>

    <!-- ========================================== -->
    <!-- SECURITY CONSTRAINTS (Optional) -->
    <!-- ========================================== -->
    <!-- Uncomment for HTTPS enforcement in production
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Secure Resources</web-resource-name>
            <url-pattern>/admin/*</url-pattern>
            <url-pattern>/staff/*</url-pattern>
            <url-pattern>/student/*</url-pattern>
        </web-resource-collection>
        <user-data-constraint>
            <transport-guarantee>CONFIDENTIAL</transport-guarantee>
        </user-data-constraint>
    </security-constraint>
    -->

    <!-- ========================================== -->
    <!-- MIME TYPES -->
    <!-- ========================================== -->
    <mime-mapping>
        <extension>css</extension>
        <mime-type>text/css</mime-type>
    </mime-mapping>
    <mime-mapping>
        <extension>js</extension>
        <mime-type>application/javascript</mime-type>
    </mime-mapping>
    <mime-mapping>
        <extension>png</extension>
        <mime-type>image/png</mime-type>
    </mime-mapping>
    <mime-mapping>
        <extension>jpg</extension>
        <mime-type>image/jpeg</mime-type>
    </mime-mapping>

    <!-- ========================================== -->
    <!-- NOTE: NO SERVLET MAPPINGS HERE! -->
    <!-- All servlets use @WebServlet annotations -->
    <!-- ========================================== -->

</web-app>